/** Route for the integrations index (list of apps), at /integrations. */

import { IntegrationsHome } from "@/features/integrations/IntegrationsHome";

export default function IntegrationsPage() {
  return <IntegrationsHome />;
}
